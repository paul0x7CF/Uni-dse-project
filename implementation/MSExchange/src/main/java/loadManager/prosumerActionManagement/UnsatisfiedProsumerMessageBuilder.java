package loadManager.prosumerActionManagement;

import protocol.Message;
import protocol.MessageBuilder;
import sendable.Bid;
import sendable.Sell;

public class UnsatisfiedProsumerMessageBuilder {
    private Message buildMessageForUnsatisfiedBidder(Bid bid) {
        MessageBuilder messageBuilder = new MessageBuilder();
        return null;
    }

    private Message buildMessageForUnsatisfiedSeller(Sell sell) {
        MessageBuilder messageBuilder = new MessageBuilder();
        return null;
    }

}
